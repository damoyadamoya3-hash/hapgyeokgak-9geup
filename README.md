# 합격각 9급

**주소 — https://damoyadamoya3-hash.github.io/hapgyeokgak-9geup/**  
폰·PC 어디서든 이 주소를 열면 바로 동작합니다. 폰에서 **홈 화면에 추가**하면 주소창 없는 앱처럼 뜨고, 한 번 열어 둔 뒤에는 **통신이 끊겨도** 풀 수 있습니다. 설치도 로그인도 필요 없고, 진도는 기기 안에 남습니다. 기기를 오갈 때는 상단 아바타 → **내 계정**에서 코드를 옮기면 됩니다.

9급 **교육행정직** 수험 준비를 게임으로 바꾼 학습 사이트. 문제 풀이·이론 암기·복습 스케줄을 한 화면에서 처리한다.

빌드 도구도, 서버도, 계정도 필요 없다. `index.html` 하나면 열린다.

---

## 무엇이 들어 있나

| 과목 | 문항 | 이론 카드 |
|---|---:|---:|
| 국어 | 209 | 8 |
| 영어 | 206 | 8 |
| 한국사 (한국사능력검정시험 대비) | 380 | 14 |
| 교육학개론 | 454 | 20 |
| 행정법총론 | 292 | 18 |
| **합계** | **1,541** | **68** |

**30개 단원 전부에 이론 카드가 있다.** 어느 단원을 눌러도 개념부터 잡고 시작할 수 있다.

문항 수에는 이론 카드의 빈칸에서 자동 생성되는 파생 문항이 포함된다.

함정포인트 219문항과 순서·계산을 묻는 94문항에는 **모두 암기 팁**이 붙어 있다. 해설은 왜 틀렸는지를 설명하지만, 다음에 같은 함정을 피하려면 한 줄로 압축된 규칙이 남아야 하기 때문이다.

### 문제 유형
- **OX** — 한 문장의 참·거짓을 즉시 판단. 헷갈리는 지점을 빠르게 훑는 용도
- **객관식** — 기출과 같은 4지선다. 지문이 붙는 독해 문항 포함
- **빈칸(cloze)** — 이론 카드의 핵심 문장에서 한 부분을 가리고 고르게 한다

모든 문항에 해설이 붙어 있고, 상당수에 암기 팁이 따로 달려 있다.

---

## 학습 모드

| 모드 | 하는 일 |
|---|---|
| 🗺️ 스토리 퀘스트 | 과목별 단원을 하나씩 클리어하며 ★을 모은다 (60%↑ 1★, 80%↑ 2★, 95%↑ 3★) |
| ⚡ OX 스피드런 | 60초 동안 최대한 많이. 과목별 또는 전 과목 랜덤. 해설을 읽는 동안에는 시간이 멈춘다 |
| 👹 보스 레이드 | 과목 보스와 HP 대결. 정답 1개 = HP 1 감소, 하트 3개를 잃으면 패배 |
| 🔁 망각곡선 복습 | 라이트너 박스로 계산한 복습 시점이 도래한 문항만 모아 푼다 |
| 📝 실전 모의고사 | 과목당 20문항 · 20분. 실제 시험처럼 **4지선다형을 먼저 채운다**. **전 과목 통합 회차**는 100문항 · 100분이며, 실전처럼 해설 없이 끝까지 풀고 마지막에 과목별로 채점한다 · **과락(40점 미만) 과목을 표시**한다 | 
| 💀 오답 지옥 | 틀린 적 있는 문항만 반복 |
| 📜 이론 도감 | 기본이론과 판례를 카드로 읽고, 바로 빈칸 드릴로 굳힌다 |
| 📕 오답노트 | 틀린 문제를 많이 틀린 순으로 훑어보고, ⭐로 따로 담아 둔다. 과목별로 걸러 보고 30개씩 끊어 읽는다 |
| 📊 학습 분석 | **모의고사 성적 추이**, 2주 학습량, 과목별 정답률, 약한 단원 Top 8, 기억 정착도 |
| 🪙 상점 | 모은 코인으로 50:50 힌트 · 하트 충전 · XP 부스터를 산다 |
| 👤 내 계정 | 이름표를 달고, 진도 전체를 한 줄 코드로 만들어 다른 기기에서 이어한다 |

학습 계획 카드는 날마다 **오늘 복습 몇 문항 · 새 문제 몇 문항**을 나눠 보여 주고, 그 칸을 누르면 바로 그 판이 시작된다. 새 문제는 가장 최근 모의고사에서 **과락(40점 미만)한 과목이 있으면 그쪽으로**, 없으면 아직 가장 덜 본 단원으로 데려간다 — 어디부터 손대질 고르는 일 자체가 시작을 미루게 하기 때문이다. 복습은 하루 목표의 절반까지만 배정해, 밀린 게 아무리 많아도 새 문제를 못 보는 날은 없다.

처음 열면 홈 맨 위에 **시작 안내**가 뜬다. 이론 카드 한 장 읽기 → 스토리 퀘스트 한 판 → 시험일 등록의 세 단계를 순서대로 짚어 주고, 각 단계를 누르면 해당 화면으로 바로 넘어간다. 20문항을 풀면 저절로 사라지고, 그 전에 닫아도 다시 나타나지 않는다.

홈 맨 위에는 **학습 계획 카드**가 있다. 설정에서 시험일을 넣으면 D-day와 함께 남은 문항을 남은 날로 나눈 **오늘의 목표 문항 수**를 계산해 준다. 목표는 하루 10~120문항 사이로 제한되며, 남은 날에 전부 보기 어려운 상황이 되면 숫자를 부풀리는 대신 약한 단원과 오답노트로 좁히라고 알려 준다.

### 이론 도감이 하는 일

행정법과 교육학은 해설만 읽어서는 체계가 잡히지 않는다. 그래서 단원별로 **비교표 · 판례 · 암기 팁**을 담은 카드를 따로 두었다.

카드에 적어 둔 핵심 문장의 빈칸은 **자동으로 실제 문항이 되어** 문제 은행에 편입된다. 즉 카드를 읽고 → 그 자리에서 빈칸으로 외우고 → 며칠 뒤 복습 큐에 다시 나타난다.

---

## 왜 게임인가

수험 공부에서 가장 어려운 건 이해가 아니라 **매일 앉는 것**이다. 그래서 다시 열게 만드는 장치를 넣었다.

- **AI 테트리스** — 옆 패널에서 AI가 혼자 블록을 쌓는다. 꽉 찬 줄은 저절로 터지지 않고 금빛으로 대기하다가, **문제를 맞혀야 터진다.** 오답이면 쓰레기 줄이 올라온다
- **잭팟 릴** — 정답마다 확률적으로 슬롯이 돌아 XP 배수(×2 ~ ×20)가 걸린다. 릴이 하나씩 순차로 멈춘다
- **FEVER 모드** — 8콤보에 진입하면 15초간 XP 2배, 화면 연출과 BGM 템포가 바뀐다
- 레벨·칭호(9급 지망생 → 전설의 수험생), 코인, 업적 27종, 일일 임무 3개(8종 중 매일 배정)
- **연속 출석 보상** — 3·7·14·30·100일 사다리와 주간 출석 스트립
- 절차적 8비트 BGM과 효과음 — 오디오 파일 없이 WebAudio로 생성

연출은 전부 코드로 만든다. 외부 이미지·영상·폰트 파일이 없어서 파일 하나로 어디서나 열린다.

### 학습 설계는 진짜다

게임 껍데기 안쪽은 교육학 교과서 그대로다.

- **간격 반복** — 라이트너 박스(0·1·2·4·7·15·30일)로 복습 시점을 계산
- **약점 우선 출제** — 정답률이 낮거나 복습 시점이 지난 문항에 높은 가중치를 주되, **가중치에 비례한 확률로 뽑는다**. 상위 몇 개를 잘라 쓰면 가중치가 조금 낮은 문항은 영영 나오지 않아 간격 반복이 무너진다
- **인출 연습** — 다시 읽기가 아니라 답을 떠올리게 하고 즉시 피드백
- **변동비율 강화** — 잭팟이 매번 터지지 않는 것이 핵심이다
- **정답 균형** — 퀘스트·모의고사·보스는 세션 안의 OX가 한쪽으로 쏠리면 같은 단원의 반대 정답 문항으로 바꿔 끼우고, OX 스피드런은 O와 X를 같은 수만큼 뽑아 섞는다. 객관식은 세션마다 선택지 순서를 다시 섞는다. 한쪽으로 찍어서 맞는 일이 없어야 인출 연습이 성립하기 때문이다

`node tools/validate.js`는 데이터 오류뿐 아니라 **정답 편향**도 함께 보고한다. OX의 O비율, 정답이 가장 긴 선택지인 비율을 출력하므로 문항을 추가할 때 기준선을 확인할 수 있다.

---

## 실행

```bash
node tools/build.js
```

`dist/index.html`이 만들어진다. 더블클릭하면 브라우저에서 바로 열린다. 개발 중에는 프로젝트 루트에서 정적 서버를 띄워도 된다.

```bash
python -m http.server 8777
```

진행도는 브라우저 `localStorage`에 저장된다. 기기를 옮길 때는 설정에서 **진행도 내보내기**로 코드를 복사해 새 기기에서 불러오면 된다.

전 문항을 학습하고 180일을 채운 상태에서도 저장 용량은 약 100KB로 `localStorage` 한도의 2% 수준이다. 일자별 기록은 180일치만 남기고 정리하므로 해가 바뀌어도 무한정 늘지 않는다. 내보내기 코드는 날짜를 사전으로 묶어 압축한다.

---

## 문항 추가하기

`data/questions/` 아래에 파일을 만들고 `data/manifest.js`에 등록하면 끝이다.

```js
QB.add([
  { id:'edu-adm-999', subject:'edu', unit:'edu-adm', type:'ox',
    src:'기출 빈출논점',
    q:'학교회계의 회계연도는 3월 1일에 시작한다.',
    a:true,
    exp:'「초·중등교육법」상 학교회계는 학년도와 일치한다.',
    tip:'학교회계 3.1~2월말 / 국가회계 1.1~12.31' }
]);
```

객관식은 `type:'mcq'`에 `choices` 배열과 정답 인덱스 `a`를 준다. 지문이 필요하면 `passage`를 붙인다.

이론 카드는 `data/theory/`에 `QB.addTheory([...])`로 추가하며, `cloze` 배열에 `{{정답}}` 표시를 넣으면 문항이 자동 생성된다.

추가한 뒤에는 반드시 검사기를 돌린다.

```bash
node tools/validate.js
```

중복 id, 정답 인덱스 범위, 선택지 중복, 해설 누락, 미등록 단원·과목, 빈칸 표시 누락을 잡아 준다. 여기에 더해

- **해설과 정답의 정합성** — OX 정답이 참인데 해설이 "반대다"로 시작하거나, 해설이 "정답은 ②"라고 적었는데 실제 인덱스가 다른 경우
- **같은 논점 중복 출제** — 지문까지 포함해 비교하므로 문두가 공용인 유형은 오탐하지 않는다
- **정답 편향** — OX의 O비율, 정답이 눈에 띄게 긴 문항 수

를 함께 보고한다.

---

## 구조

```
index.html            화면 골격
css/style.css         디자인 토큰과 전체 스타일
js/store.js           진행도 저장, 레벨, 간격 반복, 업적
js/engine.js          세션 구성과 채점
js/ui.js              화면 렌더링
js/app.js             부팅과 이벤트 배선
js/tetris.js          AI 테트리스
js/hype.js            잭팟 릴 · FEVER · 절차적 BGM
js/audio.js           효과음
js/fx.js              파티클과 토스트
data/index.js         과목·단원·보스 정의, 문제 은행 레지스트리
data/manifest.js      데이터 팩 목록
data/questions/       문항
data/theory/          이론 카드
tools/build.js        단일 파일 번들링
tools/validate.js     데이터 검사
```

---

## 접근성

- **명암 대비가 WCAG AA 기준(4.5:1)을 넘는다** — 라이트·다크 양쪽에서 검사했다. 마킹 적색은 면(배경)용과 글자용(`--brand-ink`)을 나눠 두어, 밝은 바탕 위 글자에서도 대비를 잃지 않는다
- **터치 목표가 최소 44×44px** — `pointer: coarse` 환경에서만 여백을 넓혀, 마우스 환경의 조밀한 배치는 그대로 둔다
- **폰의 뒤로가기 버튼과 제스처가 동작한다** — 앱을 나가지 않고 이전 화면으로 돌아간다. 풀이 중에 누르면 그만둘지 먼저 묻는다
- **키보드만으로 전부 조작된다** — Tab 으로 이동, Enter/Space 로 선택. OX는 `O`/`X` 또는 좌우 화살표, 객관식은 `1`~`5`, 해설 창은 Enter 로 넘긴다
- `:focus-visible` 로 키보드 이동 시에만 3px 포커스 링을 표시한다(마우스 클릭에는 나타나지 않는다)
- 아이콘만 있는 버튼에 `aria-label` 을 붙였고, 토스트 알림은 `role="status"` 로 읽힌다
- `prefers-reduced-motion` 을 존중해 FEVER 연출과 잭팟 애니메이션을 끈다

---

## 디자인

**OMR 답안지 × 야간 독서실**. 용지의 연녹빛(`#f0f3ea`), 인쇄 잉크의 남색(`#191d26`), 마킹의 적색(`#e5432b`), 형광펜의 노랑(`#f2c200`)에서 팔레트를 가져왔다. 시스템 다크 모드를 따르고, 설정에서 직접 바꿀 수도 있다.

---

## 키보드

PC 에서는 손을 마우스로 옮기지 않고 풀 수 있다. 선택지와 다음 버튼에 키가 표시된다.

| 키 | 하는 일 |
|---|---|
| `1` ~ `5` | 객관식 보기 고르기 |
| `O` / `X`, `←` / `→` | OX 답하기 |
| `Enter` 또는 `Space` | 해설을 닫고 다음 문제로 |

터치 기기에서는 키 표시가 나오지 않는다.

## 검증

```bash
node tools/validate.js   # 문항 데이터
node tools/test.js       # 동작 회귀
node tools/build.js      # 단일 파일 번들링
```

`validate.js` 는 중복 문항·정답 범위·해설 정합·OX 편향·빈칸 품질을 보고, `test.js` 는 화면 없이 동작을 본다 — 일일 임무 보상 정산, 연속 학습 계산, 밀린 복습의 순서, 모의고사 구성, 기기 간 병합, 저장 실패 처리. 여기 담긴 항목은 **전부 한 번씩 실제로 깨졌던 것**이다. 둘 다 GitHub Actions 에서 배포 전에 돌아, 하나라도 실패하면 사이트가 갱신되지 않는다.

## 문항 출처에 관하여

수록된 문항은 국가직·지방직 9급 교육행정직과 한국사능력검정시험에서 **반복 출제되는 논점**을 기준으로 새로 작성한 것이다. 특정 회차 기출문제를 그대로 옮긴 것이 아니며, 각 문항의 `src`에 근거를 표시해 두었다.

판례·결정례는 행정법 65건, 교육학 17건을 이론 카드 안에 붙여 두었고, 68장 모두에 **암기 팁과 사례**가 들어 있다(사례 총 167건). 법령과 판례는 작성 시점 기준이다. 개정이 잦은 영역(교육 법규, 행정기본법)은 실제 시험 전에 최신 조문을 확인하는 편이 안전하다.
